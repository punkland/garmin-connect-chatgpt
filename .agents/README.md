# Agent Commands

Canonical location for shared agent command templates.

- Primary path: `.agents/commands/`
- Compatibility mirror: `.claude/commands/`

When updating a command template, keep both paths synchronized.
